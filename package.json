{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "panache",
  "devDependencies": {
    "husky": "^5.1.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "prepublish": "lerna run test && lerna run lint && lerna run build --no-private"
  }
}
